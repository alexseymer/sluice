{
  "name": "sluice",
  "install": "if [ -f requirements.txt ]; then pip3 install -r requirements.txt; fi; if [ -f requirements-dev.txt ]; then pip3 install -r requirements-dev.txt; fi; if [ -f pyproject.toml ]; then pip3 install -e '.[dev]'; fi"
}
